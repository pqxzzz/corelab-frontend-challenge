import React from "react";

const Card = (title, isFavorite, description, color) => {
  return (
    <div className="bg-white max-w-[530px] rounded-3xl border border-border-primary shadow-md mb-10">
      Card
    </div>
  );
};

export default Card;
